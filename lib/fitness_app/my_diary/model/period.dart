class Period {
  DateTime from;
  DateTime to;
  int painIndex;
  int bloodIndex;
  Period({
    required this.bloodIndex,
    required this.from,
    required this.painIndex,
    required this.to,
  });
}
